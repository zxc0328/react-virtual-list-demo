import React, { useState } from "react";
import { CSSTransition } from "react-transition-group";

const TransitionItem: React.FC<{}> = () => {
  return <div className="list-item-dialog">
      
  </div>;
};

export default TransitionItem;
